# Ensure sitemap.xml is served with correct MIME type
<Files "sitemap.xml">
    Header set Content-Type "application/xml"
</Files>

# Enable compression for XML files
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE application/xml text/xml
</IfModule>